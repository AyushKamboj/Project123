---
 - hosts: localhost
   tasks:
      - docker_image:
          name: image:latest
          build: 
             path: /home/ubuntu/Project123/Dockerfile
             source: build
          state: present 
