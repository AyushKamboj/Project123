---
 - hosts: localhost
   tasks:
      - docker_image:
          name: image:latest
          build: 
             path: /root/Project123/Dockerfile
             source: build
          state: present 
